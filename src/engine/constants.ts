export const MAX_CLOCK_DIVISIONS = 10;
export const MAX_VALUE_PER_DIVISION = 5;

/** Rendering quality tier — drops node counts on weak devices (see buildCardGeometry). */
export type RenderTier = 'high' | 'balanced' | 'lite';
